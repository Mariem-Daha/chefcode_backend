# ChefCode Backend Environment Variables

# Environment mode (development or production)
ENVIRONMENT=development

# Database URL
# For local development (SQLite)
DATABASE_URL=sqlite:///./chefcode.db
# For production (PostgreSQL on Render)
# DATABASE_URL=postgresql://user:password@host:port/database

# OpenAI API Key for ChatGPT integration (optional)
OPENAI_API_KEY=your_openai_api_key_here

# Google Gemini API Key for AI features (optional)
GEMINI_API_KEY=your_gemini_api_key_here

# CORS allowed origins (comma-separated, use * for all)
# In production, set to your frontend domain(s)
ALLOWED_ORIGINS=*

# Server port (default: 8000, Render will override with PORT env var)
PORT=8000
